<<<<<<< HEAD
#include<stdio.h>
#include<math.h>
int main()
{
    double L=2.0;
    double Ac1=L*L;
    double Ar5=(2*L)*(1/4*L);
    double At2y3=((L*L)/2)*2;
    double At4=(L*(3/2*L))/2;
    double AT= Ac1+Ar5+At2y3+
printf("area total:%.2f\n",AT)
    double lado1 = sqrt(pow(L, 2) + pow(L, 2));
    double lado2 = 3 * L / 2;
    double lado3 = sqrt(pow(L, 2) + pow((3 * L / 2), 2));
    double lado4 = sqrt(pow(L / 4, 2) + pow(L, 2));
    double lado5 = 2 * L;
    double PT= lado1 + lado2 + lado3 + lado4 + lado5;
printf("perimetro total:%.2f\n",PT)
return 0;
}

=======
#include<stdio.h>
#include<math.h>
int main()
{
    double L=2.0;
    double Ac1=L*L;
    double Ar5=(2*L)*(1/4*L);
    double At2y3=((L*L)/2)*2;
    double At4=(L*(3/2*L))/2;
    double AT= Ac1+Ar5+At2y3+
printf("area total:%.2f\n",AT)
    double lado1 = sqrt(pow(L, 2) + pow(L, 2));
    double lado2 = 3 * L / 2;
    double lado3 = sqrt(pow(L, 2) + pow((3 * L / 2), 2));
    double lado4 = sqrt(pow(L / 4, 2) + pow(L, 2));
    double lado5 = 2 * L;
    double PT= lado1 + lado2 + lado3 + lado4 + lado5;
printf("perimetro total:%.2f\n",PT)
return 0;
}

>>>>>>> 0d66663 (tareas commit)
